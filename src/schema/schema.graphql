type Query {
    hello: String!
}

type Mutation {
    createUser(input: CreateUserInput): User
}

input CreateUserInput {
    name: String
    email: String
    password: String
    birthDate: String
}

type User {
    id: ID
    name: String
    email: String
    birthDate: String
}

